# A dataset artifact demonstrating the published repo layout

`demo/` — Self-contained demo (Colab-ready notebook or markdown). Run without setup.  
`src/` — Full source code, data, and outputs from the experiment execution.

**Type:** dataset  
**ID:** `gen_art_dataset_1`

## Layman Summary

This dataset artifact exists only to verify that the deployed GitHub repository uses the new human-readable folder layout.

## Full Summary

Synthetic dataset artifact for the targeted deploy-layout check. It carries a small src workspace and a self-contained demo so the deploy_gh push path can place it under the round folder named after the Test Idea phase. Synthetic dataset artifact for the targeted deploy-layout check. It carries a small src workspace and a self-contained demo so the deploy_gh push path can place it under the round folder named after the Test Idea phase. Synthetic dataset artifact for the targeted deploy-layout check. It carries a small src workspace and a self-contained demo so the deploy_gh push path can place it under the round folder named after the Test Idea phase. Synthetic dataset artifact for the targeted deploy-layout check. It carries a small src workspace and a self-contained demo so the deploy_gh push path can place it under the round folder named after the Test Idea phase. 

## Output Files

- `method_out.json`

---
*Generated by AI Inventor Pipeline*
